import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ReduxProvider } from "@/ui/components/ReduxProvider";
import { Header } from "@/ui/components/Header";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Emart - Online Shopping",
  description:
    "Emart - Your premium online shopping destination for quality products",
  icons: {
    icon: "/favicon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-black text-white min-h-screen flex flex-col`}
      >
        <ReduxProvider>
          <Header />
          <main className="flex-1 w-full pt-24 md:pt-20">{children}</main>
        </ReduxProvider>
      </body>
    </html>
  );
}
